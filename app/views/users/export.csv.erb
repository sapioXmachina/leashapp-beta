<%- headers = ["Account Name", "Name", "Email", "Role", "Last Updated", "Date Created"] -%>
<%= CSV.generate_line headers -%><%- @users_all.each do |p| -%><%= CSV.generate_line([p.account.account_name, p.name, p.email, p.role, p.updated_at, p.created_at]) -%>
<%- end -%>
